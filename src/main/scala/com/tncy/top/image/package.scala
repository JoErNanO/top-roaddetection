package com.tncy.top.image

/** Provides classes for manipulating images.
 *  
 * == Overview ==
 * Contains an [[com.tncy.top.image.ImageWrapper]] class which wraps java image objects and
 * converts them into manageable 2-D scala [[Array]] objects in the form Array[Array[Int]],
 * where each item at (column,row) in the array corresponds to a pixel RGB colour value. 
 */